Para rodar:
- uv (gerenciador de dependencias): https://docs.astral.sh/uv/
1. Instalar dependencias:

    ```bash
    uv sync
    ```
2. Rodar app:

    ```bash
    uv run python -m streamlit run <nome do app>.py
    ```
